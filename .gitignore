# Environment & secrets — never commit real keys
.env
.env.local
.env.*.local

# config.js is committed (safe public Supabase credentials only)
# config.local.js is gitignored — generated from .env, holds local API keys (Gemini etc.)
config.local.js

# Supabase CLI cache
supabase/.temp

# macOS
.DS_Store

# Node
node_modules/
.npm

# Build output
dist/
build/
.cache/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Local backups
*.backup-*
*.bak
