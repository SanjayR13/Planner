// ============================================================
//  Work Planner — Config Template
//  Copy to config.js and fill in your values,
//  OR add keys to .env and run `npm run dev` to auto-generate.
// ============================================================

window.APP_CONFIG = {

  // Supabase — cross-device sync (optional)
  SUPABASE_URL:      '',
  SUPABASE_ANON_KEY: '',
  OAUTH_PROVIDERS:   [],

  // AI overview — Gemini (get a free key at aistudio.google.com/apikey)
  GEMINI_API_KEY:    '',

  // AI overview — Anthropic alternative
  // ANTHROPIC_API_KEY: '',

};
